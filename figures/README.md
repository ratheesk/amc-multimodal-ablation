Figures are auto-generated by the notebook after training completes.

Generated files:
- ablation_bar_chart.png      — Mean ± std accuracy for all 7 configurations
- per_snr_accuracy.png        — Per-SNR accuracy curves with uncertainty bands
- accuracy_heatmap.png        — Configuration × SNR accuracy heat-map
- learning_curves_*.png       — Training convergence per configuration (one per config)
